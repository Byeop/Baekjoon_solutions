#include <bits/stdc++.h>

using namespace std;

int main(void) {
    int n;
    scanf("%d", &n);
    for (int i = 0; i < n - 2; ++i) {
        int a, b, c, d;
        scanf("%d %d %d %d", &a, &b, &c, &d);
    }

    return 0;
}